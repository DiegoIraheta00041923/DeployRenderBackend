spring:
  application:
    name: Lab3

  datasource:
    url: jdbc:postgresql://localhost:5432/lab3
    username: postgres
    password: optcgop12

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect